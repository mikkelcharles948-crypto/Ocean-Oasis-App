logo.png — official Ocean Oasis, Dominica logo (circular wave emblem + wordmark).
Used by src/components/Logo.js via require('../../assets/logo.png').

icon-square.png — app.json's icon/adaptiveIcon/splash source. Generated by
padding logo-mark.png onto a 512x512 #0B3B45 canvas (see the audit's pngjs
script) because the real logo assets aren't square, and iOS/Android icons
must be. It's a real placeholder, not final art — replace with a proper
1024x1024 icon sourced from the hotel's actual brand assets before
App Store submission; upscaling from a 110x90 source leaves it soft/pixelated
at full size.
